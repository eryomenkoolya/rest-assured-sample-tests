# Sample Rest API tests (pure Java + RestAssured)

## Introduction
The API tests are run against fake REST API - https://jsonplaceholder.typicode.com/.

## Executing the tests
Run `mvn test` from the command line.